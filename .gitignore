# dependencies
node_modules/

# machine-specific / personal - never publish
displays-detected.json
*.local
*.local.*
.env
.env.*
secrets.*
*.key
*.pem

# logs / caches / OS cruft
*.log
__pycache__/
*.pyc
.DS_Store
Thumbs.db
desktop.ini

# editor
.vscode/
.idea/
*.swp

# co-writing.md is intentionally tracked as a clean starter document.
# The app rewrites it at runtime; commit changes only when you intend to publish them.
